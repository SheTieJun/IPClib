apply plugin: 'maven'

group = 'me.shetj.sdk'
version = '1.0.0'
archivesBaseName = 'sharedMemory'

//upload
////本地上传

uploadArchives {
    repositories.mavenDeployer {
        //http://localhost:8080/maven/
        repository(url: uri('../maven/shetj'))
    }
}
